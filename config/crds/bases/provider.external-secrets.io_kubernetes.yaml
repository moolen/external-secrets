apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: kubernetes.provider.external-secrets.io
spec:
  group: provider.external-secrets.io
  names:
    categories:
    - external-secrets
    kind: Kubernetes
    listKind: KubernetesList
    plural: kubernetes
    singular: kubernetes
  scope: Namespaced
  versions:
  - name: v2alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Kubernetes defines the configuration for the Kubernetes Secret provider.
          This provider fetches secrets from Kubernetes Secrets in the same cluster.
          It's primarily useful for testing and migration scenarios.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubernetesSecretSpec defines the desired state of KubernetesSecret
              provider.
            properties:
              auth:
                description: |-
                  Auth defines how to authenticate to the Kubernetes API server.
                  If not specified, uses the provider's service account.
                properties:
                  cert:
                    description: Cert is a reference to a client certificate secret.
                    properties:
                      clientCert:
                        description: ClientCert is a reference to the client certificate.
                        properties:
                          key:
                            description: Key is the key within the secret.
                            type: string
                          name:
                            description: Name is the secret name.
                            type: string
                          namespace:
                            description: |-
                              Namespace is the secret namespace.
                              If not specified, uses the same namespace as the SecretStore.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientKey:
                        description: ClientKey is a reference to the client private
                          key.
                        properties:
                          key:
                            description: Key is the key within the secret.
                            type: string
                          name:
                            description: Name is the secret name.
                            type: string
                          namespace:
                            description: |-
                              Namespace is the secret namespace.
                              If not specified, uses the same namespace as the SecretStore.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    required:
                    - clientCert
                    - clientKey
                    type: object
                  serviceAccount:
                    description: |-
                      ServiceAccount specifies the service account to use.
                      If not specified, uses the provider pod's service account.
                    properties:
                      name:
                        description: Name is the service account name.
                        type: string
                      namespace:
                        description: |-
                          Namespace is the service account namespace.
                          If not specified, uses the same namespace as the SecretStore.
                        type: string
                    required:
                    - name
                    type: object
                  token:
                    description: Token is a reference to a bearer token secret.
                    properties:
                      key:
                        description: Key is the key within the secret.
                        type: string
                      name:
                        description: Name is the secret name.
                        type: string
                      namespace:
                        description: |-
                          Namespace is the secret namespace.
                          If not specified, uses the same namespace as the SecretStore.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
              remoteNamespace:
                description: |-
                  RemoteNamespace specifies the namespace where secrets are stored.
                  If not specified, uses the same namespace as the ExternalSecret.
                type: string
              server:
                description: |-
                  Server defines the Kubernetes API server configuration.
                  If not specified, uses in-cluster config.
                properties:
                  caBundle:
                    description: CABundle is a base64-encoded CA certificate bundle
                      for TLS.
                    format: byte
                    type: string
                  url:
                    description: |-
                      URL is the Kubernetes API server URL.
                      If not specified, uses in-cluster config.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
